export const FRONTEND_URL = process?.env?.FRONTEND_URL ?? "http://localhost:8082/api/snippets"
export const BACKEND_URL = process?.env?.SNIPPETS_SERVICE_URL
export const AUTH0_USERNAME = process?.env?.AUTH0_USERNAME ?? ""
export const AUTH0_PASSWORD = process?.env?.AUTH0_PASSWORD ?? ""
